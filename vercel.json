{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd web && corepack enable && corepack prepare pnpm@11.1.2 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd web && pnpm build",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
